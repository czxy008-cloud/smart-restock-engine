<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartrestock.mapper.SalesRecordMapper">

    <select id="selectRecentSales" resultType="com.smartrestock.entity.SalesRecord">
        SELECT * FROM t_sales_record
        WHERE sku_code = #{skuCode}
          AND store_code = #{storeCode}
          AND sales_date >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
        ORDER BY sales_date ASC
    </select>

</mapper>
